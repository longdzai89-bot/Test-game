const fs=require("fs");const {select,ask}=require("../helpers/prompt");const {printHeader}=require("../helpers/color");
module.exports=async()=>{printHeader("Menu 10 - Log realtime");const op=await select("Thao tác",["100 dòng cuối","Tìm keyword","Export","Quay lại"]);if(op==="100 dòng cuối"){try{const x=fs.readFileSync("logs/app.log","utf8").split("\n").slice(-100).join("\n");console.log(x)}catch(e){console.log(e.message)}}else if(op==="Tìm keyword"){const k=await ask("Keyword");const x=fs.existsSync("logs/app.log")?fs.readFileSync("logs/app.log","utf8"):"";console.log(x.split("\n").filter(v=>v.includes(k)).join("\n"))}};
