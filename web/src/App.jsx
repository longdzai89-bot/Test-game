import React,{useEffect,useState} from "react";import {Routes,Route,Link} from "react-router-dom";import Login from "./pages/Login.jsx";import Register from "./pages/Register.jsx";import NotFound from "./pages/NotFound.jsx";import World from "./pages/World.jsx";import "./styles/global.css";
const pageNames=["World","Character","Cultivation","Battle","Dungeon","Sect","Market","Pet","Quest","Ranking","Inventory","Notifications","Profile"];
const modules=import.meta.glob("./pages/*.jsx");
function LazyPage({name}){const [C,setC]=useState(null);useEffect(()=>{modules[`./pages/${name}.jsx`]().then(m=>setC(()=>m.default))},[name]);return C?<C/>:<div>Đang tải...</div>}
export default function App(){return <><nav style={{padding:12,display:"flex",gap:8,flexWrap:"wrap"}}><Link to="/login">Login</Link><Link to="/register">Register</Link>{pageNames.map(p=><Link key={p} to={"/"+p.toLowerCase()}>{p}</Link>)}</nav><Routes><Route path="/login" element={<Login/>}/><Route path="/world" element={<World/>}/><Route path="/register" element={<Register/>}/>{pageNames.map(p=><Route key={p} path={"/"+p.toLowerCase()} element={<LazyPage name={p}/>}/>)}<Route path="/" element={<World/>}/><Route path="*" element={<NotFound/>}/></Routes></>};