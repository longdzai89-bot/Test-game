import {useState} from 'react';
export default function Chat(){const [msg,setMsg]=useState('');const [lines,setLines]=useState(['KiemKhach: Chào mọi người!','ThầnTiên: Ai đi bí cảnh không?','BạchLong: Mình đang ở Thanh Vân Sơn.']);const send=()=>{const v=msg.trim();if(!v)return;setLines(x=>[...x,`Bạn: ${v}`].slice(-5));setMsg('')};return <div className="chat-box"><div className="chat-title">🌐 Thế giới · <span>Bang · Riêng tư</span></div><div className="chat-lines">{lines.map((x,i)=><div key={i}>{x.includes(':')?<><b>{x.split(':')[0]}</b>:{x.slice(x.indexOf(':')+1)}</>:x}</div>)}</div><div style={{display:'flex',gap:6,marginTop:8}}><input value={msg} onChange={e=>setMsg(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Nhập tin nhắn..." style={{flex:1,borderRadius:8,border:'1px solid #ffffff22',background:'#0b1420',color:'#fff',padding:'8px'}}/><button onClick={send} style={{border:0,borderRadius:8,padding:'0 12px',background:'#9b7933',color:'#fff'}}>Gửi</button></div></div>}
