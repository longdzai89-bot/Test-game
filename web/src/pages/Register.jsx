import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import api from "../api/client";import AuthBackground from "../components/auth/AuthBackground";import AuthCard from "../components/auth/AuthCard";import PasswordStrength from "../components/auth/PasswordStrength";import "../styles/auth.css";export default function Register(){const [f,setF]=useState({username:"",email:"",password:""});const nav=useNavigate();return <div className="auth"><AuthBackground/><AuthCard><h1>Đăng ký</h1><form className="form" onSubmit={async e=>{e.preventDefault();await api.post("/auth/register",f);nav("/login")}}>{Object.keys(f).map(k=><input key={k} type={k==="password"?"password":"text"} placeholder={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<PasswordStrength password={f.password}/><button>Tạo tài khoản</button></form><p>Đã có tài khoản? <Link to="/login">Đăng nhập</Link></p></AuthCard></div>}