import {useState} from "react";import {useNavigate,Link} from "react-router-dom";import {useAuth} from "../store/authStore";import "../styles/auth.css";import AuthBackground from "../components/auth/AuthBackground";import AuthCard from "../components/auth/AuthCard";export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState("");const nav=useNavigate();const login=useAuth(s=>s.login);return <div className="auth"><AuthBackground/><AuthCard><h1>Đăng nhập</h1><form className="form" onSubmit={async e=>{e.preventDefault();await login({email,password});nav("/")}}><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Mật khẩu" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button>Đăng nhập</button></form><p>Chưa có tài khoản? <Link to="/register">Đăng ký</Link></p></AuthCard></div>}