import {create} from "zustand";import api from "../api/client";export const useAuth=create(set=>({user:null,token:localStorage.getItem("token"),login:async(data)=>{const r=await api.post("/auth/login",data);localStorage.setItem("token",r.data.data.token);set({token:r.data.data.token,user:r.data.data.user})},logout:()=>{localStorage.removeItem("token");set({token:null,user:null})}}));